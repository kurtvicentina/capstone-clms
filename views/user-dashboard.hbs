<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <style>
        body {
            background-color: #f5f5f5;
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
        }

        .container-sm {
            background-color: #fff;
            border: 1px solid #ccc;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
        }

        .card {
            border: 1px solid #ccc;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
        }

        .card-header {
            background-color: #007bff;
            color: #fff;
        }

        .btn-primary {
            background-color: #007bff;
            border: 1px solid #007bff;
        }

        hr {
            border: 1px solid #ccc;
        }
    </style>
</head>
<body>
    <h1 class="pt-4" style="text-align: center;">Welcome {{username}}!</h1>

    <div class="container-sm mx-auto">
        <h3>Search books</h3>
        <form class="d-flex" method="post" action="/" novalidate>
            <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name="search">
            <button class="btn btn-primary" type="submit">Search</button>
        </form>
    </div>

    <div class="card text-center mx-auto" style="width: 70%;">
        <div class="card-header">
            Featured Book
        </div>
        <div class="card-body">
            <h5 class="card-title"><strong>{{featuredBook}}</strong></h5>
            <p class="card-text">by {{featuredBookAuthor}} <br>
            Copies available: {{featuredBookCopies}}
            </p>
            <a href="/viewuserbook" class="btn btn-primary">Go to book list</a>
        </div>
        <div class="card-footer text-muted">
            Featured Today
        </div>
    </div>

    <hr>

    <div class="container d-flex justify-content-between">
        <div class="card" style="width: 45%;">
            <h5 class="card-header">Recently Added Book</h5>
            <div class="card-body">
                <h5 class="card-title"></h5>
                <p class="card-text"><strong>{{recentlyAddedBook}}</strong> by
                <strong>{{recentlyAddedBookAuthor}}</strong> <br>
                Copies available: {{recentlyAddedBookCopies}}
                </p>
                <a href="/viewuserbook" class="btn btn-primary">Go to book list</a>
            </div>
        </div>

        <div class="card" style="width: 45%;">
            <h5 class="card-header">All-time Favorite Book</h5>
            <div class="card-body">
                <h5 class="card-title"></h5>
                <p class="card-text"><strong>{{favoriteBook}}</strong> by
                <strong>{{favoriteBookAuthor}}</strong><br>
                Copies available: {{favoriteBookCopies}}
                </p>
                <a href="/viewuserbook" class="btn btn-primary">Go to book list</a>
            </div>
        </div>
    </div>
</body>
</html>
